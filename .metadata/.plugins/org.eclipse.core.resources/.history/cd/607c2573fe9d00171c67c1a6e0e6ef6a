package beakjun;

public class main_4 {

	public static void main(String[] args) {
		// TODO Auto-generated method stub
		int[] n = { 2, 9, 10, 21, 24 };
		

		for (int index = 1; index < n.length; index++) {
			int temp = n[index];
			int aux = index - 1;
			
			while (n[aux] > temp || temp < 10) {
				if(temp < 10) {
					if((n[aux]%10) < temp) {
						break;
					}
				}
				n[aux + 1] = n[aux];
				aux--;
			}
			n[aux + 1] = temp;
		}
		
		/*
		for (int i = 1; i < n.length; i++) {
			int key = n[i];
			for (int j = i - 1; j >= 0; j--) {
				if (n[j] > 9) {
					int valD, valN;
					valD = n[j] / 10;
					valN = n[j] % 10;
					if (valD == key) {
						if (valN >= key) {
							n[j] = key;
							break;
						}
					} else {
						if (valD >= key) {
							n[j] = key;
							break;
						}
					}
					n[j + 1] = n[j];
				} else {
					if (n[j] >= key) {
						n[j] = key;
						break;
					}
					n[j + 1] = n[j];
				}
			}

		}*/

		for (int i = 0; i < n.length; i++) {
			System.out.println(n[i]);
		}
	}

}
